{
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
  "start": "npm-run-all --parallel server client",
  "server": "cd server && npm run dev",
  "client": "cd client && npm run dev"
}
}
